{
  "name": "hermes-pet",
  "version": "0.1.0",
  "private": true,
  "description": "hermes-pet M0 — Windows 桌面宠物（Electron + 原生 HTML/CSS/JS，无构建步骤，除 electron 外零依赖）",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "smoke": "electron . --smoke-test",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "electron": "44.4.5"
  }
}
