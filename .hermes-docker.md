# hermes-pet 与 hermes-pet-dev 容器

## 一句话

容器 `hermes-pet-dev` 是**跑零依赖纯逻辑**（`node --check` / `node --test`）的地方；
**GUI 只能在 Windows 宿主上跑**，不要在容器里试图启动 Electron。

## 为什么 GUI 不在容器里跑

| 事实 | 后果 |
|---|---|
| 容器是 Linux，没有 X11/Wayland 显示器，也没有 Windows 桌面会话 | `electron .` 起不来；透明无边框窗口、托盘、`screen.getPrimaryDisplay()` 在无显示器环境里没有意义 |
| 桌宠依赖 Windows 宿主能力：托盘图标、`app.setLoginItemSettings`（开机自启）、多显示器 workArea、`alwaysOnTop` 置顶 | 这些 API 只有在真实 Windows 桌面会话里才有正确语义 |
| 素材与纯逻辑都在挂载卷里，容器读到的就是宿主看到的同一份文件 | 容器里跑测试 = 宿主上跑同一份代码，不需要把 Electron 搬进容器 |

所以分工是：

- **容器**：`node --check` 语法体检 + `node --test` 纯逻辑单测（取帧 / 状态机 / 调度器 / 配置 / 适配器）
- **宿主**：`npx electron . --smoke-test`、`npx electron . --self-check` 做 GUI 自证

## 容器用法

```powershell
# 容器启动时把本仓库挂到 /workspace
docker exec -w /workspace hermes-pet-dev node --version

# 1) 语法体检
docker exec -w /workspace hermes-pet-dev node --check src/main.js
docker exec -w /workspace hermes-pet-dev sh -c "for f in src/main.js src/preload.js src/core/*.js src/adapters/*.js src/renderer/*.js; do node --check \"$f\" || exit 1; done"

# 2) 纯逻辑单测（零依赖，不需要 npm install）
docker exec -w /workspace hermes-pet-dev node --test tests/
```

容器里**不需要** `npm install`：`tests/` 只加载 `src/core/` 与 `src/adapters/`，这些模块除 Node 内置模块（`fs` / `path` / `os`）外没有任何依赖，也不 `require('electron')`。

> 注：单测脚本 `npm test` 在 Windows 宿主（Node 24）上用的是 `node --test tests/*.test.js`；
> Node 20/22 里直接 `node --test tests/` 也可以，两种写法在容器里都验证过。

## 常见误用

- 不要在容器里 `npm install` / `npx electron .`：前者白装（GUI 用不上），后者必然失败。
- 不要在容器里 `node src/main.js`：主进程第一行就 `require('electron')`，会直接报错，这是设计使然。
- 验证纯逻辑：容器里 `node --test tests/`。
- 验证 GUI：Windows 宿主上 `npx electron .`（或 `--smoke-test` / `--self-check`）。

## 宿主上的 Electron 安装备忘

```powershell
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
npm install --registry=https://npmmirror.com
```

若 `npm install` 只装到 JS 包、没下二进制（`node_modules\electron\dist\` 不存在），用兜底配方：

```powershell
$v = (Get-Content node_modules\electron\package.json -Raw | ConvertFrom-Json).version
$zip = "$env:TEMP\electron-v$v-win32-x64.zip"
curl.exe -sL -o $zip "https://npmmirror.com/mirrors/electron/$v/electron-v$v-win32-x64.zip"
Expand-Archive $zip -DestinationPath "node_modules\electron\dist" -Force
& "node_modules\electron\dist\electron.exe" --version   # 必须打印 v44.4.5
```

本机（本轮实测）`npm install` 确实只装到了 JS 包，最后是用 `%TEMP%\electron-prewarm\node_modules\electron\dist` 拷贝补上的，效果与上面的 zip 配方一致。