# 派单任务书：hermes-pet M0 功能集设计（给 脑洞🧠）

## 你是谁 / 任务性质
你是高级科学家（脑洞），本次不是学术讨论，是**产品功能设计**。

## 背景（自包含，不需要问）
站长（王嘉仪/阿斯兰）要开发「琉斯计划」的桌宠前端产品 **hermes-pet**，今天要在一个小时内验收一个**能跑起来、有陪伴感的 MVP**。
- 项目目录：`D:\Jiayi\Projects\hermes-pet`
- 已有产品设计文档 v0.1（1027 行，含 10 个用户旅程场景、状态矩阵、交互范式、设计系统、技术约束附录）：`D:\Jiayi\Projects\hermes-pet\hermes-pet-product-design.md`
- 已有代码：仅 PySide6 玩具骨架（62+52 行），本轮会重做，你不需要考虑它
- 已定技术选型（不要推翻）：**Electron + 原生 HTML/CSS/JS（无构建步骤）**，透明无边框置顶窗口，精灵图动画（素材：oneko 猫精灵图 32x32）
- 今天不接真 LLM 后端（只做可插拔 adapter + 本地 mock）

## 必读材料（用 terminal 读，你没有 read_file 工具）
```
Get-Content -Path "D:\Jiayi\Projects\hermes-pet\hermes-pet-product-design.md" -Encoding UTF8 -Raw
```
重点读：第一层用户旅程（场景 1-10）、第二层状态矩阵、第三层交互范式、4.1 气质定义、4.7 角色外观。

## 你要产出什么
一份功能集设计文档，落盘到：`D:\Jiayi\Projects\hermes-pet\docs\M0-features.md`

内容要求：
1. **M0 功能集（8-12 条）**——每条按下面格式：
   - 功能名
   - 用户可见表现（一两句话，能想象出画面）
   - 为什么这条进 M0（对「陪伴感」这个核心价值的贡献）
   - 验收方式（站长打开应用后怎么算这条生效了）
2. **互相咬合的行为设计**：桌宠的关键不是功能列表而是**节奏**——它什么时候主动说话、什么时候闭嘴。给出明确规则（主动说话频率上限、不回应后的行为、被打断时的行为）。这部分要写具体数字。
3. **MVP 明确砍掉的**（5-8 条）——每条一句为什么砍（是技术成本？还是它其实破坏陪伴感？）
4. **「下一个最该做的 3 件事」**——M0 之后的方向排序，各一句理由。
5. 若你发现产品设计文档 v0.1 里有**自相矛盾或明显做不成**的地方，单列一节指出（≤5 条）。

## 硬约束
- M0 必须能在 **Electron 单窗口 + 原生 JS** 内实现，不引入构建工具、不引入 UI 框架、不引入 npm 依赖（Electron 本身除外）
- 所有主动行为必须是**本地计时驱动**，不依赖网络
- 不要设计多角色、语音、Live2D/VRM、向量记忆——这些明确不在 M0
- 不要写实现代码，只写功能设计与验收口径

## 交付纪律
- 你没有 write_file 工具，用 terminal 写文件。长中文文本用 PowerShell here-string + 显式 UTF-8：
  ```powershell
  $p = "D:\Jiayi\Projects\hermes-pet\docs\M0-features.md"
  $s = @'
  ...内容...
  '@
  [System.IO.File]::WriteAllText($p, $s, (New-Object System.Text.UTF8Encoding($false)))
  ```
- 文本里的引号一律用「」，**不要用半角双引号**
- **禁止在 `D:\Jiayi\` 根目录新建任何文件或文件夹**（站长铁律）
- 写完只回 **≤10 行摘要**（写了什么、几条功能、你砍了什么、你的反调），**不要把全文回在聊天里**
